import { IfcEventData, IfcWorkerAPI, SubsetWorkerAPI, WorkerAPIs } from '../BaseDefinitions';
import { SubsetManager } from '../../components/SubsetManager';
import { BvhManager } from '../../components/BvhManager';
import { Serializer } from '../serializer/Serializer';
export declare class SubsetWorker implements SubsetWorkerAPI {
    private worker;
    private bvh;
    private serializer;
    subset?: SubsetManager;
    API: WorkerAPIs;
    constructor(worker: IfcWorkerAPI, bvh: BvhManager, serializer: Serializer);
    getSubset(data: IfcEventData): Promise<void>;
    createSubset(data: IfcEventData): Promise<void>;
    removeSubset(data: IfcEventData): Promise<void>;
    private initialize;
}
